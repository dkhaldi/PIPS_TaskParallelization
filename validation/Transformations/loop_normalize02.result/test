int conan(int n)
{
   int j = 0, i = n;
   for(i = 1; i <= i; i += 1)
      j += 1;
   i = -1*pips_max(2, i, 0)+i;
   return j;
}
int conan(int n)
{
   int j = 0, i = n;
   for(i = 1; i <= i; i += 1)
      j += 1;
   i = i-pips_max(2, i, 0);
   return j;
}
