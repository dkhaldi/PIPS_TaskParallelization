// with two symbolique argument
// with just the callee function cannot say anything
// base for more complexe examples, call_max01..., not done :(
//   to test the function sc_minmax_pvector 8 cases to do (for max)

int foo(int n, int m) {
  int i;
  int a[100];
  for(i=0; i<100; i++)
    if (i<n && i<m)
        a[i] = 0;
  return 0;
}
