/* Do we have separate name spaces for struct, union and enum as
 * implemented in PIPS internal representation?
 *
 * Correct version of struct34.c
 */

struct s {                                                              /*0007*/
   int i;
};

union u {                                                               /*0009*/
   int i;
};

enum e {i};                                                             /*0011*/
