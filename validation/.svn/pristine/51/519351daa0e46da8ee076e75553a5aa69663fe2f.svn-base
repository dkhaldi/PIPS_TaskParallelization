/* Issue with omp.h: attribute lists not supported
 *
 * Note: omp.h does not seem to have been copied in validation. its
 * content may change from machine to machine. This is not portable.
 */

#include "omp.h"

int main () {
  return 0;
}
