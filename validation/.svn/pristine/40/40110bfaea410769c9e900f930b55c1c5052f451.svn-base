/* Use of the NULL constant. It is never dereferenced in this pece of code.
 *
 * The error would appear, for instance, with "return *p;"
 */

#include<stdlib.h>

int main()
{
  int i, j=0;
  int *p, *q;
  p = NULL;
  
  q = p+(1+j);
  i = q-p;
  
  return 0;
}
