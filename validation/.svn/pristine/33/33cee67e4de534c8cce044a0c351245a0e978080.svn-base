// to test the function sc_minmax_pvector 8 cases to do (for max)
// need to done the other 7 cases...

// This example is not fine to test sc_minmax_pvector, because the regions/preconditions already to the job
// Need to introdce if?
int main() {
  int n, m, result = -1;
//   n=10;
//   m=20;
//   result = foo(n, m);
  
  if (n<m)
    result = foo(n, m);
  
  return result;
}

//function from argument05
int foo(int n, int m) {
  int i;
  int a[100];
  for(i=n; i<100; i++)
    if (i>n && i>m)
        a[i] = 0;
  
//   for(i=0; i<n; i++)
//     if (i<m)
//         a[i] = 0;
    
//   for(i=0; i<n; i++)
//     if (i<n && i<m)
//         a[i] = 0;
    
//   for(i=0; i<m; i++)
//     a[i] = 0;
//   for(i=0; i<n; i++)
//     a[i] = 0;
    
  return 0;
}
