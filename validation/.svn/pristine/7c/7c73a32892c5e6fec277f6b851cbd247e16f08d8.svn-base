/* This piece of code is not correct. On purpose. Check error recovery
   when workspace creation fails at the external syntax check by gcc. */

//#include <stdio.h>

int main() {
  typedef char toto;
  int toto = 0;
  //printf("%d\n", toto);
  return toto;
}
