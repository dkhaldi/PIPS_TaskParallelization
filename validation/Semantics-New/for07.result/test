
"DISTRIBUTER"

void for07(int n)
{
   int a[n], b[n];
   {
      int i;
      for(i = 0; i <= n-1; i += 1) {
         a[i] = i;
      }
      for(i = 0; i <= n-1; i += 1) {
         typedef int mytype;
         mytype x;
         x = rand();
         b[i] = x;
      }
   }
}
