/*
 * THIS FILE HAS BEEN AUTOMATICALLY GENERATED BY NEWGEN.
 *
 * PLEASE DO NOT MODIFY IT.
 */

typedef void * Pvecteur;

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "genC.h"
#include "message.h"


/* MESSAGE
 */
message copy_message(message p) {
  return (message) gen_copy_tree((gen_chunk*) p);
}
void free_message(message p) {
  gen_free((gen_chunk*) p);
}
message check_message(message p) {
  return (message) gen_check((gen_chunk*) p, message_domain);
}
bool message_consistent_p(message p) {
  check_message(p);
  return gen_consistent_p((gen_chunk*) p);
}
bool message_defined_p(message p) {
  return gen_defined_p((gen_chunk*) p);
}
list gen_message_cons(message p, list l) {
  return gen_typed_cons(MESSAGE_NEWGEN_DOMAIN, p, l);
}
void message_assign_contents(message r, message v) {
  check_message(r);
  check_message(v);
  message_assert("defined references to domain message",
                 message_defined_p(r) && message_defined_p(v));
       memcpy(r, v, sizeof(struct _newgen_struct_message_));
}
void message_non_recursive_free(message p) {
  // should clear up contents...
  free(p);
}
void write_message(FILE* f, message p) {
  gen_write(f, (gen_chunk*) p);
}
message read_message(FILE* f) {
  return (message) gen_read(f);
}
message make_message(entity a1, list a2, Pvecteur a3, list a4) {
  return (message) gen_alloc(5*sizeof(gen_chunk), GEN_CHECK_ALLOC, message_domain, a1, a2, a3, a4);
}

