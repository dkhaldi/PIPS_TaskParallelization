/*
 * THIS FILE HAS BEEN AUTOMATICALLY GENERATED BY NEWGEN.
 *
 * PLEASE DO NOT MODIFY IT.
 */


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "genC.h"
#include "kernel_memory_mapping.h"

/* MEMORY_MAPPING
 */
memory_mapping copy_memory_mapping(memory_mapping p) {
  return (memory_mapping) gen_copy_tree((gen_chunk*) p);
}
void free_memory_mapping(memory_mapping p) {
  gen_free((gen_chunk*) p);
}
memory_mapping check_memory_mapping(memory_mapping p) {
  return (memory_mapping) gen_check((gen_chunk*) p, memory_mapping_domain);
}
bool memory_mapping_consistent_p(memory_mapping p) {
  check_memory_mapping(p);
  return gen_consistent_p((gen_chunk*) p);
}
bool memory_mapping_defined_p(memory_mapping p) {
  return gen_defined_p((gen_chunk*) p);
}
list gen_memory_mapping_cons(memory_mapping p, list l) {
  return gen_typed_cons(MEMORY_MAPPING_NEWGEN_DOMAIN, p, l);
}
void memory_mapping_assign_contents(memory_mapping r, memory_mapping v) {
  check_memory_mapping(r);
  check_memory_mapping(v);
  message_assert("defined references to domain memory_mapping",
                 memory_mapping_defined_p(r) && memory_mapping_defined_p(v));
       memcpy(r, v, sizeof(struct _newgen_struct_memory_mapping_));
}
void memory_mapping_non_recursive_free(memory_mapping p) {
  // should clear up contents...
  free(p);
}
void write_memory_mapping(FILE* f, memory_mapping p) {
  gen_write(f, (gen_chunk*) p);
}
memory_mapping read_memory_mapping(FILE* f) {
  return (memory_mapping) gen_read(f);
}
memory_mapping make_memory_mapping(set a) {
  return (memory_mapping) gen_alloc(2*sizeof(gen_chunk), GEN_CHECK_ALLOC, memory_mapping_domain, a);
}

