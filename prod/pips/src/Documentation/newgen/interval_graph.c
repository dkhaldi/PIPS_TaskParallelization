/*
 * THIS FILE HAS BEEN AUTOMATICALLY GENERATED BY NEWGEN.
 *
 * PLEASE DO NOT MODIFY IT.
 */


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "genC.h"
#include "interval_graph.h"

/* INTERVAL_VERTEX_LABEL
 */
interval_vertex_label copy_interval_vertex_label(interval_vertex_label p) {
  return (interval_vertex_label) gen_copy_tree((gen_chunk*) p);
}
void free_interval_vertex_label(interval_vertex_label p) {
  gen_free((gen_chunk*) p);
}
interval_vertex_label check_interval_vertex_label(interval_vertex_label p) {
  return (interval_vertex_label) gen_check((gen_chunk*) p, interval_vertex_label_domain);
}
bool interval_vertex_label_consistent_p(interval_vertex_label p) {
  check_interval_vertex_label(p);
  return gen_consistent_p((gen_chunk*) p);
}
bool interval_vertex_label_defined_p(interval_vertex_label p) {
  return gen_defined_p((gen_chunk*) p);
}
list gen_interval_vertex_label_cons(interval_vertex_label p, list l) {
  return gen_typed_cons(INTERVAL_VERTEX_LABEL_NEWGEN_DOMAIN, p, l);
}
void interval_vertex_label_assign_contents(interval_vertex_label r, interval_vertex_label v) {
  check_interval_vertex_label(r);
  check_interval_vertex_label(v);
  message_assert("defined references to domain interval_vertex_label",
                 interval_vertex_label_defined_p(r) && interval_vertex_label_defined_p(v));
       memcpy(r, v, sizeof(struct _newgen_struct_interval_vertex_label_));
}
void interval_vertex_label_non_recursive_free(interval_vertex_label p) {
  // should clear up contents...
  free(p);
}
void write_interval_vertex_label(FILE* f, interval_vertex_label p) {
  gen_write(f, (gen_chunk*) p);
}
interval_vertex_label read_interval_vertex_label(FILE* f) {
  return (interval_vertex_label) gen_read(f);
}
interval_vertex_label make_interval_vertex_label(list a) {
  return (interval_vertex_label) gen_alloc(2*sizeof(gen_chunk), GEN_CHECK_ALLOC, interval_vertex_label_domain, a);
}

