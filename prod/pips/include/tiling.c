/*
 * THIS FILE HAS BEEN AUTOMATICALLY GENERATED BY NEWGEN.
 *
 * PLEASE DO NOT MODIFY IT.
 */

typedef void * Pvecteur;
typedef void * matrice;

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "genC.h"
#include "tiling.h"



/* TILING
 */
tiling copy_tiling(tiling p) {
  return (tiling) gen_copy_tree((gen_chunk*) p);
}
void free_tiling(tiling p) {
  gen_free((gen_chunk*) p);
}
tiling check_tiling(tiling p) {
  return (tiling) gen_check((gen_chunk*) p, tiling_domain);
}
bool tiling_consistent_p(tiling p) {
  check_tiling(p);
  return gen_consistent_p((gen_chunk*) p);
}
bool tiling_defined_p(tiling p) {
  return gen_defined_p((gen_chunk*) p);
}
list gen_tiling_cons(tiling p, list l) {
  return gen_typed_cons(TILING_NEWGEN_DOMAIN, p, l);
}
void tiling_assign_contents(tiling r, tiling v) {
  check_tiling(r);
  check_tiling(v);
  message_assert("defined references to domain tiling",
                 tiling_defined_p(r) && tiling_defined_p(v));
       memcpy(r, v, sizeof(struct _newgen_struct_tiling_));
}
void tiling_non_recursive_free(tiling p) {
  // should clear up contents...
  free(p);
}
void write_tiling(FILE* f, tiling p) {
  gen_write(f, (gen_chunk*) p);
}
tiling read_tiling(FILE* f) {
  return (tiling) gen_read(f);
}
tiling make_tiling(matrice a1, Pvecteur a2) {
  return (tiling) gen_alloc(3*sizeof(gen_chunk), GEN_CHECK_ALLOC, tiling_domain, a1, a2);
}

